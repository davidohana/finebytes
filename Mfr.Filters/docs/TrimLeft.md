# TrimLeft

Removes a fixed **count** of characters from the **start** of the segment.

**Example**

- Count `3`: `ABCDEF` → `DEF`

Use **TrimLeft** when you want to **drop** a known number of characters from the start. To keep only a prefix of a given length, see [ExtractLeft](ExtractLeft.md).
