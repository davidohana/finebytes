# TrimRight

Removes a fixed **count** of characters from the **end** of the segment.

**Example**

- Count `2`: `ABCDEF` → `ABCD`

Use **TrimRight** when you want to **drop** a known number of characters from the end. To keep only a suffix of a given length, see [ExtractRight](ExtractRight.md).
