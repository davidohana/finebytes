# RemoveSpaces

Deletes **every** occurrence of the current word-separator character (no gaps left). The separator comes from [SpaceCharacter](SpaceCharacter.md) when used earlier in the chain (default is ordinary space).

**Example**

- Separator is space: `a b c` → `abc`
